{
    "setup": {
        "delete_setup_files": true,
        "delete_readme": false
    },
    "electron": {
        "folder": "$HOME/.electron"
    },
    "config": {
        "applications_folder": "$ELECTRON/applications",
        "config_file": "$ELECTRON/.configrc",
        "secrets_folder": "$HOME/Dropbox/.electron",
        "system_startup_script": "$ELECTRON/startup/startup.py"
    }
}